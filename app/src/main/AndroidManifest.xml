<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nammahomestay">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NammaHomeStay"
        android:usesCleartextTraffic="true">

        <activity
            android:exported="true"
            android:name=".data.remote.RoleSelectionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.host.OwnerDashboardActivity" />
        <activity android:name=".ui.host.AddHomestayActivity" />
        <activity android:name=".ui.host.HostLoginActivity" />
        <activity android:name=".ui.host.HostRegisterActivity" />
        <activity android:name=".ui.host.EnquiryActivity" />
        <activity android:name=".ui.host.EditMenuActivity" />
        <activity android:name=".ui.host.AddDishActivity" />
        <activity android:name=".ui.host.ReplyActivity" />
        <activity android:name=".ui.host.AddSecretSpotActivity" />
        <activity android:name=".ui.host.MyHomestaysActivity" />
        <activity android:name=".ui.host.EditHomestayActivity" />
        <activity android:name=".ui.host.ManageBookingsActivity" />

        <activity android:name=".ui.customer.CustomerHomeActivity" />
        <activity android:name=".ui.customer.CustomerLoginActivity" />
        <activity android:name=".ui.customer.CustomerRegisterActivity" />
        <activity android:name=".ui.customer.CustomerInquiryActivity" />
        <activity android:name=".ui.customer.HomestayDetailActivity" />
        <activity android:name=".ui.customer.CustomerBookingsActivity" />
        <activity android:name=".ui.customer.ChatActivity" />

    </application>

</manifest>
